# rcd

Rust fuzzy `cd` searcher. Uses `fzf` to index and search through system files, then, instantly navigate go them.

## Install

Clone the repository and run the `install.sh` script. It will automatically install it to your shell config.

Then,

```sh
# Scroll through and hit enter to automatically navigate to the directory.
~ rj
```
